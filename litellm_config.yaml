# DEPRECATED: This file is deprecated.
# LLM routing configuration has moved to config.yaml (llm section).
# This file will be removed in a future version.
#
# Migration: Move your model_list to config.yaml llm section,
# or set LITELLM_CONFIG env var to point to a new YAML file.

# ===================================（已废弃）
# LiteLLM Router 配置文件（已废弃，请迁移到 config.yaml llm 段）
# ===================================

model_list:
  # --- DeepSeek V4 系列 ---
  - model_name: deepseek-v4-flash
    litellm_params:
      model: deepseek/deepseek-v4-flash
      api_key: "os.environ/DEEPSEEK_API_KEY"

  - model_name: deepseek-v4-pro
    litellm_params:
      model: deepseek/deepseek-v4-pro # 映射到 deepseek 官方接口
      api_key: "os.environ/DEEPSEEK_API_KEY"


router_settings:
  routing_strategy: simple-shuffle
  num_retries: 2
